<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm sticky-top py-2">
  <div class="container">
    <!-- Brand -->
    <a class="navbar-brand fw-bold text-primary fs-4" href="/">Blogify</a>

    <!-- Mobile Toggle -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav Links -->
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <!-- Left links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/">Home</a>
        </li>

        <% if (locals.user) { %>
          <li class="nav-item">
            <a class="btn btn-outline-light fw-semibold ms-2 px-3" href="/blog/add-new">+ Add Blog</a>
          </li>
        <% } %>
      </ul>

      <!-- Right aligned user/auth links -->
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (locals.user) { %>
          <!-- Profile dropdown -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button"
              data-bs-toggle="dropdown" aria-expanded="false">
              <img src="<%= user.profileImageURL %>" 
                   alt="profile" 
                   class="rounded-circle me-2 border border-2 border-primary" 
                   style="width:35px; height:35px; object-fit:cover;">
              <span class="fw-semibold"><%= user.fullName.split(" ")[0] %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow-sm">
              <li><a class="dropdown-item" href="/blog/myBlogs">📚 My Blogs</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger fw-semibold" href="/user/logout">🚪 Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <!-- Auth buttons -->
          <li class="nav-item">
            <a class="btn btn-outline-light fw-semibold px-3" href="/user/signup">Create Account</a>
          </li>
          <li class="nav-item ms-2">
            <a class="btn btn-primary fw-semibold px-3" href="/user/signin">Sign In</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
